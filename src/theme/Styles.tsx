const flexRowStart = {
    flex: 1,
    flexDirection: "row",
    alignItems: "flex-start",
};

const flexRowCenter = {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
};

const flexRowEnd = {
    flex: 1,
    flexDirection: "row",
    alignItems: "end",
};

const flexColumnStart = {
    flex: 1,
    flexDirection: "column",
    alignItems: "flex-start",
};

const flexColumnCenter = {
    flex: 1,
    flexDirection: "column",
    alignItems: "center",
};

const flexColumnEnd = {
    flex: 1,
    flexDirection: "column",
    alignItems: "flex-end",
};

export default {
    flexRowStart,
    flexRowCenter,
    flexRowEnd,
    flexColumnStart,
    flexColumnCenter,
    flexColumnEnd,
};
